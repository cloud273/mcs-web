import 'package:clinic/util/localization.dart';
import 'package:flutter/material.dart';

class ListPackagePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Package'.localized),
      ),
      body: Container(),
    );
  }
}
